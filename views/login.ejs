<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="navbar-left">
            <a href="/">Home</a>
            <a href="/blog/archive">Archive</a>
        </div>
        <div class="navbar-right">
            <% if (user) { %>
                <a href="/login" onclick="logout(event)">Logout</a>
            <% } else { %>
                <a href="/register">Register</a>
                <a class="active" href="/login">Login</a>
            <% } %>
        </div>
    </nav>
            
    <% if (!user) { %>
        <h1 class="centered">Log in to your account</h1>
        <form id="loginForm" onsubmit="login(event)">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" required placeholder="Enter your email">
            
        <br><br>
        
        <label for="password">Password:</label>
        <input type="password" id="password" name="password" required placeholder="Enter your password">
        
        <br><br>
        
        
        <button type="submit">Login</button>
    </form>

    <p id="infoMessage"></p>
    <% } else { %>
        <h1 class="centered">You're already logged in</h1>
        <p class="centered">
            <span><a href="/" class="href">Go back to homepage</a></span>
            <span>or</span>
            <span><a href="/login" onclick="logout()">Log out</a></span>
        </p>
    <% } %>
        
    <script src="/js/app.js"></script>
</body>
</html>
